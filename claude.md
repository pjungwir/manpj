# Commands

`/btw` - to ask a quick side question

`use isolation` - from patrick:

> Wow, just discovered this? Wanna do a bunch at once, work that might normally conflict in the same session? Don't start a new session. Ask Claude to do it with an "agent in isolation". This will create a temp git worktree and allow agents in the same session to deconflict. The Claude agent manager will then orchestrate them and get everything back into one branch.

